# Qui va il geojson del luogo reale

Prodotto nella **chat geografia** (vedi `../README.md`, il brief). Atteso:
`<stato>.geojson` (FeatureCollection, coordinate WGS84 lat/lon) con le proprietà di
`../convenzioni/schema_luogo.md`. Il viewer in `viewer/` va poi ripuntato su questo file.
